# Daily Push - 每日资讯精选

Apple 风格的每日资讯聚合网站，展示 AI 热点、万代/Hot Toys 模玩新品、游戏折扣信息。

## 特性

- **每日 00:00 自动更新** - 比 skill 推送更早发布当日资讯
- **Apple 风格设计** - 简洁优雅的界面，支持深色模式
- **三大板块** - AI 热点 TOP10、模玩新品、游戏折扣
- **响应式布局** - 完美适配手机、平板、桌面
- **自动部署** - GitHub Actions 每天自动构建部署

## 快速开始

```bash
# 安装依赖
npm install

# 生成当日数据
npx tsx scripts/fetch-daily-news.ts

# 开发模式
npm run dev

# 生产构建
npm run build
```

访问 [http://localhost:3000](http://localhost:3000)

## 自动更新机制

网站采用 **双时间线** 更新策略：

| 时间 | 动作 | 数据来源 |
|------|------|---------|
| **00:00** | 网站自动生成并部署 | 日期驱动算法（内容每日轮播） |
| **09:00** | skill 推送真实热点 | AI 搜索（可选手动同步到网站） |

### 方案 1：全自动（推荐）

GitHub Actions 已配置每天北京时间 00:00 自动：
1. 运行 `fetch-daily-news.ts` 生成数据
2. 构建 Next.js 静态站点
3. 部署到 GitHub Pages

**无需任何操作，网站每天自动刷新。**

### 方案 2：本地更新

```bash
# 生成数据
npx tsx scripts/fetch-daily-news.ts

# 构建
npm run build

# 本地预览
python3 -m http.server 3000 --directory dist
```

### 方案 3：同步 skill 数据（可选）

09:00 skill 推送后，如需更新网站：

```bash
npx tsx scripts/update-from-skill-push.ts "📰 今日 AI 热点..."
npm run build
```

## 项目结构

```
daily-push-web/
├── app/                          # Next.js 应用
│   ├── components/               # 页面组件
│   │   ├── AINewsSection.tsx     # AI 热点板块
│   │   ├── ToysSection.tsx       # 模玩板块
│   │   └── GamesSection.tsx      # 游戏折扣板块
│   ├── page.tsx                  # 主页面
│   └── layout.tsx                # 根布局
├── lib/
│   └── data.ts                   # 生成的数据文件
├── scripts/
│   └── fetch-daily-news.ts       # 数据生成脚本
└── .github/workflows/
    └── daily-update.yml          # 自动部署配置
```

## 技术栈

- **框架** - Next.js 14 + React 18
- **语言** - TypeScript
- **样式** - Tailwind CSS
- **UI 组件** - shadcn/ui
- **图标** - Lucide Icons
- **部署** - GitHub Pages + GitHub Actions

## 数据来源

网站数据通过算法每日生成，确保：
- 每天 00:00 有新鲜内容
- 热点主题按日期轮播
- 商品、折扣信息定期更新

如需真实实时数据，可结合 skill 的 09:00 推送手动更新。

## 部署

### GitHub Pages（推荐）

1. Fork 本仓库
2. 启用 GitHub Pages（Settings > Pages > Source: GitHub Actions）
3. 每天 00:00 自动部署

### 手动部署

```bash
npm run build
# 将 dist 文件夹上传到任意静态托管服务
```

## 自定义

编辑 `scripts/fetch-daily-news.ts` 修改：
- AI 热点主题列表
- 万代/Hot Toys 商品库
- Steam 游戏折扣信息

## License

MIT
