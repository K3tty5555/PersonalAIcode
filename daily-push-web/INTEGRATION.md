# 每日推送网页更新方案

## 概述

网站每日 **00:00** 自动更新，比 skill 的 9:00 推送更早发布当日资讯。

## 时间线

```
00:00 ─────▶ 网站自动生成当日资讯（日期驱动算法）
09:00 ─────▶ skill 推送真实热点（可选二次更新）
```

## 数据流

```
┌─────────────────┐     ┌─────────────────┐     ┌─────────────────┐
│  00:00 自动脚本  │────▶│   lib/data.ts   │────▶│  网站部署上线   │
│ (日期驱动生成)   │     │   (每日更新)    │     │  (GitHub Pages) │
└─────────────────┘     └─────────────────┘     └─────────────────┘
                               ▲
                               │ (可选)
┌─────────────────┐            │
│  09:00 skill   │────────────┘
│   推送真实数据  │   (手动更新)
└─────────────────┘
```

## 使用方法

### 方案 1：每日 00:00 自动更新（默认）

GitHub Actions 已配置每天北京时间 00:00 自动运行：

```yaml
# .github/workflows/daily-update.yml
schedule:
  - cron: '0 16 * * *'  # UTC 16:00 = 北京时间 00:00
```

脚本 `scripts/fetch-daily-news.ts` 会：
- 基于日期自动生成当日热点资讯（内容按天轮播）
- 更新 `lib/data.ts`
- 自动构建并部署到 GitHub Pages

**无需任何操作，网站每天 00:00 自动刷新。**

---

### 方案 2：本地手动更新

```bash
cd daily-push-web

# 生成当日数据
npx tsx scripts/fetch-daily-news.ts

# 构建网站
npm run build

# 本地预览
python3 -m http.server 3000 --directory dist
```

---

### 方案 3：09:00 skill 推送后更新（可选）

如果希望用 skill 的真实推送数据替换 00:00 的生成数据：

```bash
cd daily-push-web

# 将 skill 推送文本传入脚本
npx tsx scripts/update-from-skill-push.ts "📰 2月27日 AI热点 TOP10 **1️⃣..."

# 重新构建
npm run build
```

或手动编辑 `lib/data.ts` 后直接构建。

---

## 文件结构

```
daily-push-web/
├── scripts/
│   ├── fetch-daily-news.ts       # 主脚本：每日 00:00 生成数据
│   ├── update-from-skill-push.ts # 辅助：从 skill 推送更新
│   └── generate-from-skill.ts    # 旧版（保留备用）
├── lib/
│   └── data.ts                   # 生成的数据文件
└── .github/workflows/
    └── daily-update.yml          # 00:00 自动部署
```

## 数据生成逻辑

`fetch-daily-news.ts` 根据日期自动轮播内容：

| 数据类型 | 生成方式 | 更新频率 |
|---------|---------|---------|
| AI 热点 | 基于日期选择主题轮播 | 每天 00:00 |
| 万代商品 | 8 款产品每天轮播展示 3 款 | 每天 00:00 |
| Hot Toys | 6 款产品每天轮播展示 3 款 | 每天 00:00 |
| Steam 折扣 | 8 款游戏每天轮播展示 4 款 | 每天 00:00 |
| PS 折扣 | 固定 4 款热门游戏 | 每两周更新 |
| Nintendo | 暂无优惠提示 | 每周检查 |

## 本地开发

```bash
# 安装依赖
npm install

# 生成数据
npx tsx scripts/fetch-daily-news.ts

# 开发模式
npm run dev

# 生产构建
npm run build

# 预览构建结果
python3 -m http.server 3000 --directory dist
```

## 注意事项

1. **00:00 数据**：基于日期算法生成，确保每天都有新鲜内容
2. **skill 数据**：09:00 推送更精准，可手动更新替换
3. **部署**：推送到 GitHub 自动触发 Actions，或手动上传 dist 文件夹到任意静态托管

## 访问地址

- 本地: `http://localhost:3000`
- GitHub Pages: `https://你的用户名.github.io/daily-push-web`
